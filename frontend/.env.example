# Frontend env (copy to .env.local)
VITE_API_URL=http://localhost:4000

# Supabase — public values, safe in the browser bundle
VITE_SUPABASE_URL=https://<ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon key from Supabase Settings → API>

# Demo "Sign in as recruiter" credentials — public on purpose (demo account only)
VITE_DEMO_EMAIL=recruiter@yeldo-demo.app
VITE_DEMO_PASSWORD=demo123
